# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Search.index
POST        /                    controllers.Search.fillLand
POST        /filter              controllers.Search.filter
POST        /fastFilter          controllers.Search.fastFilter
POST        /addUser             controllers.Search.addUser
POST       /checkAdded           controllers.Search.checkAdded
# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)

    